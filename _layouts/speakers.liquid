---
layout: default
---

<h2 class="display-2--lighter">Our speakers, panelists, and workshop presenters</h2>

{% assign speakers = site.data.speakers | sort %}

<ol class="toc toc__speakers">
{% for speaker_hash in speakers %}
{% assign speaker = speaker_hash[1] %}
  <li><a href="#{{ speaker.name | slugify }}">{{ speaker.name }}</a></li>
{% endfor %}
</ol>

<h2 class="display-2--lighter">Speaker details</h2>

<ol class="speaker-details">
{% for speaker_hash in speakers %}
{% assign speaker = speaker_hash[1] %}
  <li class="speaker">
    <h2 class="speaker__id" id="{{ speaker.name | slugify }}">
      <img class="speaker__id__hackergotchi" src="{{ speaker.hackergotchi }}" alt="hackergotchi of {{ speaker.name }}" />


      {% if speaker.twitter %}
      <a class="speaker__id__name" href="https://twitter.com/{{ speaker.twitter }}">
        {{ speaker.name }}
      </a>
      {% elsif speaker.website %}
      <a class="speaker__id__name" href="{{ speaker.website }}">
        {{ speaker.name }}
      </a>
      {% else %}
      <span class="speaker__id__name">
        {{ speaker.name }}
      </span>
      {% endif %}

    </h2>

    {% if speaker.whois %}
    <div class="lede lede--speaker">
      <span class="speaker__whois">
        {{ speaker.whois | markdownify | remove: '<p>' | remove: '</p>' }}
      </span>
    </div>
    {% endif %}

    {% if speaker.panel == true %}

    <span class="speaker__event-type speaker__event-type--panel">
      Panelist
    </span>

    {% endif %}


    {% if speaker.talk %}

    <h3>
      <span class="speaker__event-type">
        Talk
      </span>
      {{ speaker.talk.title | markdownify | remove: '<p>' | remove: '</p>' }}
    </h3>

    <div class="speaker__talk-abstract">
      {{ speaker.talk.abstract | markdownify }}
    </div>

    {% endif %}

    {% if speaker.workshop %}

    <h3>
      <span class="speaker__event-type">
        Workshop
      </span>
      {{ speaker.workshop.title | markdownify | remove: '<p>' | remove: '</p>' }}
    </h3>

    <div class="speaker__workshop-abstract">
      {{ speaker.workshop.abstract | markdownify }}
    </div>

    {% endif %}

    <a class="skip-link" href="#content">&uarr; Return to top of content</a>
  </li>
{% endfor %}
</ol>
